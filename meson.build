project('PvZ', 'cpp')
deps = [ dependency('ncurses'), dependency('entt') ]
inc = include_directories('include')
executable(
  'PvZ', 
  'src/main.cc', 
  dependencies : deps, 
  sources : files(
  ),
  include_directories : inc
)
